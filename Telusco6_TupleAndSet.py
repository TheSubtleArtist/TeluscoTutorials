

tup = (21, 24, 31, 54, 42, 13, 15)
#Tuples are recognized by the parenthesis
#Tuples store multiple values in a single variable
#tuples are ordered, unchangeable, and allow duplicate values

print(tup)
print(tup[1])
print(len(tup))


#Sets are used to store multiple items in a single variable.
#A set is a collection which is both unordered and unindexed.
#Sets are written with curly brackets.
#Set uses "hash" which attempts to be fast, which means no duplication

set1 = {22, 25, 42, 21, 33, 87, 61, 12, 31, 22}
print(set1)
print("notice that 22 is printed only once")
